'use strict';

const { ApiError } = require('./ApiError');

class AuthError extends ApiError {
    constructor(message = 'Unauthorized. Invalid or missing API key.', body = {}) {
        super(message, 'ERR_UNAUTHENTICATED', 401, body);
        this.name = 'AuthError';
    }
}

module.exports = { AuthError };
